<div class="container">
  <div class="row justify-content-center">
    <div class="col-12">
      <h1>All articles 💥</h1>
      <% @articles.each do |article| %>
      <h2 class="background-light"><%= link_to article.title, article_path(article) %></h2>
      <p><%= article.content %></p>
      <% end %>
    </div>
  </div>
</div>
